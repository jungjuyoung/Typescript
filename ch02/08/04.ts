interface Person {
  first: string
  last: string
}
const p: Person = { first: 'Jane', last: 'Jacobs' }
//    -           --------------------------------- Values
//       ------ Type

// :뒤에 오는것은 타입, = 뒤에 오는것들은 값.

export default {}
