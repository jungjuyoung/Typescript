const axis1: string = 'x' // Type is string
const objConst = { name: 'typescripts' } // Type is {name: string}
let objLet = {name: 'EcamScripts'} // Type is{ name: string;}
const axisConst = 'x' // Type is x
let axislLet = 'x' // Type is string
axislLet = 9
const axis2 = 'y' // Type is "y"
// const는 값을 변경할 수 없어서 할당한 값의 타입이 그대로 타입으로 적용.

export default {}
